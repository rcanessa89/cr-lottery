export enum DrawTime {
  MORNING,
  AFTERNOON,
}

export enum Product {
  CHANCES = 'chances',
  LOTERIA = 'loterianacional',
  LOTTO = 'lotto',
  MONAZOS = 'tresmonazos',
  TIEMPOS = 'nuevostiempos',
}
